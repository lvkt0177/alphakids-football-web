name: Alpha Kids Football Club
description: Premium Sports Editorial - a black, white, and gold academy site for parents, built on a full greenfield redesign with bento grids and mesh gradients.
colors:
  ink: "#101010"
  ink-soft: "#4A4A4A"
  ink-faint: "rgba(16, 16, 16, 0.12)"
  paper: "#FAFAFA"
  paper-alt: "#F1F0F0"
  accent: "#ffc200"
  accent-dark: "#ebbc21"
  accent-text: "#815D03"
  accent-soft: "#F6EFD9"
  white: "#FFFFFF"
  hero-empty-glow: "#232323"
  hero-mesh-gold: "rgba(201, 162, 39, 0.22)"
  hero-scrim-black: "rgba(0, 0, 0, 0.97)"
  cta-hover-ink: "#050505"
  mobile-menu-scrim: "rgba(34, 27, 51, 0.5)"
typography:
  display-hero:
    fontFamily: "Archivo, Be Vietnam Pro, sans-serif"
    fontSize: "clamp(2.8rem, 6.4vw, 4.8rem)"
    fontWeight: 900
    lineHeight: 0.92
  display-hero-accent:
    fontFamily: "Archivo, Be Vietnam Pro, sans-serif"
    fontSize: "clamp(3.8rem, 9.6vw, 7.6rem)"
    fontWeight: 900
    lineHeight: 0.92
  display:
    fontFamily: "Archivo, Be Vietnam Pro, sans-serif"
    fontSize: "clamp(2.2rem, 3.6vw, 3.2rem)"
    fontWeight: 800
    lineHeight: 1.05
  display-sm:
    fontFamily: "Archivo, Be Vietnam Pro, sans-serif"
    fontSize: "clamp(1.5rem, 2.4vw, 1.9rem)"
    fontWeight: 800
  display-cta:
    fontFamily: "Archivo, Be Vietnam Pro, sans-serif"
    fontSize: "clamp(2.1rem, 4vw, 3rem)"
    fontWeight: 800
  display-letter:
    fontFamily: "Archivo, Be Vietnam Pro, sans-serif"
    fontSize: "clamp(2.8rem, 5.4vw, 4.4rem)"
    fontWeight: 900
  display-index:
    fontFamily: "Archivo, Be Vietnam Pro, sans-serif"
    fontSize: "1.15rem"
    fontWeight: 800
  display-timeline-num:
    fontFamily: "Archivo, Be Vietnam Pro, sans-serif"
    fontSize: "clamp(3.4rem, 7vw, 6rem)"
    fontWeight: 900
  display-timeline-heading:
    fontFamily: "Archivo, Be Vietnam Pro, sans-serif"
    fontSize: "clamp(1.8rem, 3vw, 2.6rem)"
    fontWeight: 800
  display-club-intro:
    fontFamily: "Archivo, Be Vietnam Pro, sans-serif"
    fontSize: "clamp(2rem, 3.2vw, 2.8rem)"
    fontWeight: 800
  quote:
    fontFamily: "Archivo, Be Vietnam Pro, sans-serif"
    fontSize: "clamp(1.4rem, 2.4vw, 1.9rem)"
    fontWeight: 700
  subtitle:
    fontFamily: "Be Vietnam Pro, sans-serif"
    fontSize: "clamp(1rem, 1.7vw, 1.15rem)"
    fontWeight: 600
  lead:
    fontFamily: "Be Vietnam Pro, sans-serif"
    fontSize: "18px"
    fontWeight: 600
  lead-alt:
    fontFamily: "Be Vietnam Pro, sans-serif"
    fontSize: "17px"
    fontWeight: 500
  quote-glyph:
    fontFamily: "Archivo, Be Vietnam Pro, sans-serif"
    fontSize: "4.5rem"
    fontWeight: 900
  body:
    fontFamily: "Be Vietnam Pro, sans-serif"
    fontSize: "16px"
    fontWeight: 500
    lineHeight: 1.7
  body-sm:
    fontFamily: "Be Vietnam Pro, sans-serif"
    fontSize: "14.5px"
    fontWeight: 500
  label:
    fontFamily: "Be Vietnam Pro, sans-serif"
    fontSize: "14px"
    fontWeight: 600
  small:
    fontFamily: "Be Vietnam Pro, sans-serif"
    fontSize: "13.5px"
    fontWeight: 500
  caption:
    fontFamily: "Be Vietnam Pro, sans-serif"
    fontSize: "12.5px"
    fontWeight: 500
  micro:
    fontFamily: "Be Vietnam Pro, sans-serif"
    fontSize: "11.5px"
    fontWeight: 700
  logo-text:
    fontFamily: "Be Vietnam Pro, sans-serif"
    fontSize: "19px"
    fontWeight: 800
  menu-close:
    fontFamily: "Be Vietnam Pro, sans-serif"
    fontSize: "20px"
    fontWeight: 400
rounded:
  sm: "8px"
  md: "14px"
  lg: "20px"
  xl: "28px"
  toast: "10px"
  pill: "999px"
  legacy-chip: "2px"
spacing:
  section: "120px"
  section-mobile: "64px"
components:
  button-primary:
    backgroundColor: "{colors.accent}"
    textColor: "{colors.ink}"
    rounded: "{rounded.md}"
    padding: "14px 28px"
  button-outline:
    backgroundColor: "transparent"
    textColor: "{colors.ink}"
    rounded: "{rounded.md}"
    padding: "14px 28px"
---

# Design System: Alpha Kids Football Club

## Overview

**Creative North Star: "Premium Sports Editorial"**

This is a Full Greenfield Redesign (v3) that discards the prior "Modern Sports Editorial" identity's purple accent entirely and rebuilds the color and motion system from zero, while keeping the layout-family thinking that pass had already earned (bento grids, sticky-scroll stack, manifesto list, carousels - see Layout). The palette is now black, white, and gold: near-black ink, off-white paper, and exactly one metallic-gold accent, chosen deliberately over the more predictable "football club color" defaults (red, blue) because gold reads as trophy, medal, and academic excellence, matching "học viện cao cấp" (premium academy) positioning more precisely than a team-kit hue.

**Dials (design-taste-frontend methodology): `DESIGN_VARIANCE 9` · `MOTION_INTENSITY 8` · `VISUAL_DENSITY 3`.** Set per explicit user instruction for this greenfield pass ("bứt phá hoàn toàn khỏi mọi layout SaaS truyền thống" / break completely from conventional SaaS layouts). Variance 9 means the page leans harder into bento asymmetry, mesh-gradient depth, and oversized editorial type than the v2 pass (variance 8). Motion 8 adds a genuine scroll-linked mesh drift in the hero and a gold "light-catch" shimmer sweep on primary CTAs, on top of the parallax, sticky-scroll rail, reveal-stagger, and carousel already established.

Confirmed visual rejections: the purple accent and every token built on it; the whiteboard/marker world from the pass before that (dashed arrows, hand-marker type, dot-grid texture); a repeated "N equal items in a row" grid used across more than one section (Section-Layout-Repetition Ban, unchanged from v2); a feature-bullet checklist inside the hero; a colored `border-left`/`border-right` "side-tab" accent on any card (caught by the project's own detector as the single most recognizable AI-slop tell - the video quote panel was fixed to use a large decorative quote glyph instead).

**Key Characteristics:**
- Off-white ground (`--paper`) and near-black ink (`--ink`, never pure `#000`) as the two structural tones
- One locked gold accent, split into two contrast-safe roles (see Colors, this is not two accents, it is one hue used correctly): `--accent` (#ffc200) for fills/icons/large text on dark grounds, `--accent-text` (#815D03) for gold text on light grounds
- A single display type family (Archivo, weights 800–900) for all headings, pushed to a larger editorial scale than the prior pass; Be Vietnam Pro carries every other role
- **Every section owns a distinct layout family** - bento, sticky-scroll stack, manifesto list, split, banner, horizontal carousel, pull-quote - none repeated (unchanged from v2, re-verified after the palette rebuild)
- A layered, purposeful motion system: page-load fade+slide for the hero, a scroll-linked parallax on the hero photo, a slow ambient drift on the hero's mesh-gradient scrim, scroll-triggered fade+slide-with-stagger for section entry, a `position: sticky` panel stack with a real scroll-progress rail for the "4 tư duy" journey, a horizontal scroll-snap carousel for the two browsable lists, and a gold shimmer sweep on primary-button hover

## Colors

Two structural neutrals (ink, paper) plus one accent hue used through two contrast-calibrated roles. Nothing else carries meaning.

### Primary
- **Ink** (#101010): near-black, never pure black. Body text, headings, hero/C.A.R.E/footer backgrounds, default button outline/text-on-gold.
- **Accent - Gold (fill role)** (#ffc200): backgrounds, icon fills, borders, large display text on dark grounds (C.A.R.E letters, hero "TƯ DUY," closing-CTA band). **Never used as text color on a light background** - measured at 1.55:1 against `--paper`, below WCAG AA even at the large-text 3:1 threshold.
- **Accent - Gold (text role)** (#815D03): the same hue, darkened specifically so it passes AA as text on light grounds (5.74:1 against `--paper`, 5.27:1 against `--paper-alt`). Used for the `.hl` heading highlight, pillar/FAQ numerals and icons, and any other gold text sitting on `--paper` or `--paper-alt`.

### Named Rules
**The Two-Gold Rule.** This system has one accent hue, not two accents - but it has two roles, and mixing them up is a contrast bug, not a style choice. Ask "what is gold sitting on, and is it text or a fill?" before picking which token: dark ground → `--accent` is safe for text; light ground → text must be `--accent-text`, fills/icons/borders may still use `--accent`. Button text on a gold fill is always `--ink` (11.75:1), never white (1.62:1, fails AA) and never `--accent-text` (would be nearly invisible on its own hue).

### Neutral
- **Paper** (#FAFAFA): the default page ground.
- **Paper Alt** (#F1F0F0): alternate section background for rhythm - used sparingly.
- **Ink Soft** (#4A4A4A): secondary/muted text (8.49:1 against paper).
- **Ink Faint** (rgba(16,16,16,.12)): hairline dividers and borders, the primary separation device instead of card shadows.
- **White** (#FFFFFF): text on ink or ink-on-white contexts (footer button text, outline-invert hover).
- **Hero mesh tones**: `hero-empty-glow` (#232323, no-banner fallback radial), `hero-mesh-gold` (a soft gold bloom at 22% opacity, positioned away from the text zone), `hero-scrim-black` (the mesh's darkest stop) - components of the hero's layered scrim, not freestanding UI colors.

### Named Rules (continued)
**The Color Consistency Lock.** One accent hue, everywhere, unchanged - see The Two-Gold Rule above for how its two roles are chosen correctly rather than turning it into two accents in practice.
**The Reserved-Strength Exception.** The closing CTA band runs gold at full-bleed coverage (background, not just a button) because it is the page's single most important ask. Text on it is ink (7.87:1) per the button-text rule above.

## Typography

**Display Font:** Archivo (with Be Vietnam Pro, sans-serif fallback) - verified Vietnamese-subset support directly from Google Fonts' served CSS before adoption.
**Body Font:** Be Vietnam Pro (with sans-serif fallback).

**Character:** One confident geometric grotesk for every heading, pushed to a larger editorial scale in this pass (hero title now clamp(2.8rem,6.4vw,4.8rem)/clamp(3.8rem,9.6vw,7.6rem), up from v2's clamp(2.4rem,...)/clamp(3.2rem,...)), paired with a plain, highly legible humanist grotesk for body copy. No handwritten, script, or second display face anywhere.

### Hierarchy
- **Display Hero** (900, clamp(2.8rem,6.4vw,4.8rem) / clamp(3.8rem,9.6vw,7.6rem), 0.92): the hero's "BÓNG ĐÁ" / "TƯ DUY" wordmark.
- **Display** (800, clamp(2.2rem,3.6vw,3.2rem), 1.05): section headings.
- **Display Small** (800, clamp(1.5rem,2.4vw,1.9rem)): FAQ teaser heading.
- **Display accents** (800–900, 1.15rem–4.4rem): C.A.R.E letters, pillar/timeline numerals, closing-CTA heading, club-intro heading - each scaled to its own container, not the page hierarchy.
- **Lead** (600, 18px): tier-card titles.
- **Body** (500, 14.5–16px, 1.7): running copy; near-identical steps across components are intentional, not drift.
- **Label** (600–700, 11–14px, often uppercase): nav, tags, captions, card titles.

### Named Rules
**The One Display Face Rule.** Archivo is the only display/heading face on the page. Mixing in a second display personality is the exact defect two redesigns ago that this system exists to prevent - never reintroduce it.

## Layout

Sections alternate `paper` and `paper-alt` for rhythm, used sparingly. One section (C.A.R.E) and the highlight strip run a solid `ink` background - the system's dark bands. The hero is full-bleed `100dvh`, breaking the container grid; every other section respects `--container-max` / `--section-gap` (120px desktop / 64px mobile - widened this pass for "khoảng trắng cao cấp," premium whitespace).

### Named Rules
**Section-Layout-Repetition Ban (mandatory, carried over from v2 and re-verified after the palette rebuild).** Once a layout family is used for a section, it does not reappear elsewhere on the page:
- **Hero** - full-bleed 3-layer (photo / mesh-gradient scrim / text)
- **Highlight strip** - inline text row divided by hairlines
- **Split** (club intro) - 2-column image/text, now the page's #2 section (see Reality & Business Fit Audit note below)
- **Large centered stage + caption** (video) - a single wide (`1040px` cap, not full-bleed) 16/9 video stage centered in its section, a pull-quote as a centered caption underneath rather than a competing column; the page's #3 section, deliberately smaller than the Hero so no second section ties it for the strongest visual moment (see Components)
- **Sticky-scroll stack** (hành trình) - 4 full-width panels that pin and cover each other on scroll, with a scroll-linked progress rail and a 4-dot step indicator; each panel's active state surfaces its matching pillar icon, a drawn-on-active underline, and its C.A.R.E cross-reference tag (see Components)
- **Manifesto list** (C.A.R.E) - full-width stacked rows on a dark ground, alternating horizontal offset; optionally paired with a sticky 2-column photo rail when Admin has uploaded images (falls back to the original centered 1-column list when they haven't - see Components)
- **Horizontal carousel** (class tiers, activities) - scroll-snap; the one family intentionally reused twice, since both are genuinely browsable lists. Both carousels end in a non-item tile (a consult CTA / a "view all" link) that fills leftover width on wide screens instead of leaving dead space, styled to read as a card, not a decoration
- **Spotlight coverflow** (Vì sao phụ huynh chọn Alpha Kids) - full-bleed 100vw breakout, one testimonial centered and in focus, the previous/next quote peeking at each edge blurred and faded into the page ground, clickable to jump; auto-advances continuously (no manual pause control, a deliberate exception to the reduced-motion default for this signature section) with dot indicators for direct navigation; replaces the earlier flex-wrap proof-point/testimonial grid
- **Boxed Q&A** (Còn thắc mắc về Alpha Kids?) - light-ground band with icon + text head, a full accordion of admin-managed FAQs below it, closing in a "view all" link - not a bare teaser banner
- **Bold Solid Gold Statement** (closing CTA) - full-bleed edge-to-edge (100vw breakout, not container-bound), solid `--accent` fill (the only section on the page that uses gold as a background, not an accent), oversized left-aligned editorial headline - no photo, no card, no glass
**The No-Side-Tab Rule.** A colored `border-left`/`border-right` on a card or panel is banned outright - it is the single most recognizable AI-generated-UI tell. Mark a quote, callout, or emphasis block with a decorative glyph, a background tint, or typographic weight instead.

## Elevation & Depth

Flat by default. No card shadows in the grid components - separation comes from hairline dividers (`ink-faint`). Soft ambient shadows (`--shadow-sm`/`--shadow-md`) are reserved for genuinely floating elements: the sticky header, the mobile menu panel, the video play button. The hero's sense of depth comes from its mesh gradient, not a shadow.

### Named Rules
**The Hairline-Over-Shadow Rule.** Where a bordered-card-with-shadow instinct shows up, use a shared hairline divider inside a flush grid instead.

## Shapes

A small geometric radius scale, no asymmetric "hand-drawn" shapes: `--radius-sm` (8px), `--radius-md` (14px, buttons), `--radius-lg` (20px, photos/cards/video stage, boxed accordion items), `--radius-xl` (28px, the FAQ page's closing "not found your answer" band). The closing CTA is a deliberate exception to the radius scale: full-bleed edge-to-edge with **no** border-radius, since a rounded corner would contradict its "breaks out of the grid" role. `--radius-toast` (10px), `--radius-pill` (999px), and a 2px "legacy chip" radius (mobile-menu close, footer logo) are pre-existing system-utility shapes, unrelated to either world's language. Perfect circles (`50%`) are used freely for icon roundels, the timeline numerals/icon badges, and the timeline dot cluster.

## Components

### Buttons
- **Shape:** simple rounded rectangle (`--radius-md`, 14px), 1.5px border.
- **Primary (`.btn--accent`):** solid gold fill, **ink text** (never white - see The Two-Gold Rule), with a gold "light-catch" shimmer sweep animating across on hover (a diagonal white-tinted gradient translating through, `prefers-reduced-motion`-gated) - the one purely decorative motion in the system, justified as a literal material cue (light catching a metallic surface).
- **Outline (`.btn--outline` / `.btn--outline-invert`):** transparent, ink or white border depending on ground; hover inverts to a solid ink/white fill.
- **This is the one sitewide button style** - header, footer, hero, and every CTA share it.

### Reality & Business Fit Audit - page reorder + section removal
A dedicated audit (not a visual redesign) found the homepage front-loaded pedagogy: two sections back-to-back explained the same 4 "tư duy" pillars (a full-bleed scroll-scrub stage right after Hero, then the sticky-scroll stack "hành trình" later), with zero trust-building content between the Hero and the first pedagogy block, and no way for a parent to find a nearby branch from the homepage at all. Fix, in order of what changed:
1. **Removed the full-bleed scroll-scrub pillars section entirely** (previously the page's #2 section, a `height:420vh` pinned scroll-jack stage - 4 redesigns deep: Swap-in-place Bento → Fixed-list dark → Framed-Photo click-driven → Full-bleed scroll-scrub). Kept the sticky-scroll stack ("Sticky-scroll stack" family, still the page's #4 section) as the sole presentation of the 4 pillars - it's the cheaper mechanism (pure CSS `position:sticky` stacking, no custom scroll-jack math) and removing the earlier, longer, more front-loaded duplicate does more to fix the pacing problem than removing the later one would.
2. **Moved "Split" (club intro)** from #3 to #2 - trust content (a real club, real people) now comes immediately after the Hero/highlight strip, before any pedagogy section.
3. **Moved and redesigned the video section** from #6 to #3, right after club intro - see Components below. Two trust-building sections (club intro + video) now sit back-to-back before the page turns to pedagogy (pillars journey, C.A.R.E).
4. **Added a branch-finder link** to club intro (`.club-intro__branch-link`, "Xem cơ sở gần bạn") pointing at `route('branch.index')` - a light-touch fix for parents having no way to check branch proximity from the homepage; deliberately a text link, not a second button, so it doesn't compete with the section's primary CTA.

### Large Centered Stage + Caption (video)
Replaced the previous 2-column bento (video fighting a pull-quote column for width) - then redesigned from a light-ground/two-headline version (an `<h2>` plus a same-weight pull-quote stacked above the stage, read as two competing headlines) into a single-voice hierarchy, then redesigned that hierarchy's *roles* once more (see Redesign 5 below). The stage fills a centered column inside the normal `.container` - deliberately **not** full-bleed like the Hero or closing CTA (a second section tied with the Hero for strongest visual moment would flatten the hierarchy, and would reuse the full-bleed family a third time - see Section-Layout-Repetition Ban). Grew from 1040px to **1280px** on request for more presence, still short of `--container-max` (1500px).

**What the background tried and rejected before landing.** Two earlier passes at giving the flat `--ink` background some depth were both built, verified, and then rejected on taste grounds, not technical grounds:
- **Vector accent shapes** (a solid gold corner shard + a hairline ring breaking the stage's corners) - correct in isolation but tonally wrong: the brand's whole video argument is "real footage, not description," and a CSS-drawn accent shape is exactly the graphic device that argument exists to avoid.
- **A photographic section background** (`bg-section-video.png`, a heavily blurred stadium-floodlights-at-night bokeh photo, bright clusters at the far edges, center column near-black) - measured and verified (contrast ratios ~20.8:1 / ~10.6:1 against WCAG's 4.5:1/7:1, re-checked at 1400px and 1920px) but still rejected: felt heavy as a whole-section treatment and cut abruptly against the light section that follows.

**Redesign 4 - the split section, current.** Back to flat color (no image, no vector decoration - the restrained material this system actually speaks), but the `--ink` fill no longer covers the whole section. `.intro-video-section__dark-bg` is an absolutely-positioned overlay from the section's top, its `height` driven entirely by a JS-measured CSS custom property (`--video-split-height`), not a guessed percentage: `initVideoSplit()` (`home.js`) measures the video stage's real rendered top offset and height and sets the split to *stage-top + stage-height × 2/3* - i.e. the boundary always falls exactly two-thirds down the video card itself, regardless of how many lines the eyebrow/statement text wraps to at a given viewport width (a quantity no fixed CSS percentage could predict). Below that line the section is plain `--paper` - and because A4 (the pillars sticky-scroll stack right after) is also `--paper`, the boundary between the two sections disappears entirely; only a dark band floats over the video's top two-thirds, not a full dark block. `.intro-video__caption`, which now always lands in the white zone, switched from dimmed white to `--ink-soft`.

The measurement has one real timing hazard, caught and fixed by testing rather than assumed correct: `.intro-video-feature` carries `.reveal`, which holds it at `translateY(32px)` until scrolled into view. A naive single measurement on `DOMContentLoaded` captures that pre-reveal offset and freezes the split ~25px too low - reproduced and confirmed via a scripted check before the fix. The fix listens for the reveal's own `transitionend` (the precise moment the offset settles) plus a `scroll` listener as a self-healing fallback, rather than a fixed delay guess.

**Play button - tried a custom one, then removed it for native controls.** The stage originally had a hand-drawn gold circle button (`.intro-video__play`) that JS swapped for an iframe/`<video>` on click. Its first shadow (`--shadow-md`, `rgba(16,16,16,.12)`) was calibrated for light-ground cards and composited into a barely-visible muddy ring on the `#232323` stage - reported back as "xấu" (ugly), confirmed from a screenshot, and fixed with a dark lift-shadow plus a gold ambient glow tuned for a dark ground. That fix shipped, then the user asked to drop the custom button entirely and use the platform's own default control instead. `.intro-video__play`, its shadow work, and `initIntroVideo()` (the click-handler that injected the iframe/`<video>`) are gone. The media element now renders directly server-side: `<video controls poster="...">` for upload mode (the browser draws its own native play button, scrubber, and volume/fullscreen controls - nothing left to style or fix), and a plain `<iframe>` (no `autoplay` param) for YouTube mode, which shows YouTube's own thumbnail and play chrome. `.intro-video__stage` keeps its frame (border, radius, `#232323` fallback background) but dropped `cursor: pointer` - the frame isn't a click target anymore, only the native controls are.

**Play hint overlay - augmenting native controls, not replacing them again.** In real use the user reported not seeing a play button at all. A Playwright screenshot of the live page (matched against a screenshot the user sent from their own browser) confirmed the native control bar was in fact rendering correctly - the issue was legibility, not function: Chrome's own play glyph is a small triangle in the bottom-left of the control strip, easy to lose against a busy poster image (the current thumbnail is a bold "FOOTBALL HIGHLIGHT" graphic). `.intro-video__play-hint` adds a large (76px) semi-transparent dark circle with a white play triangle, centered on the stage, purely as a discoverability affordance - it does not reimplement playback: clicking it just calls `video.play()` (`initVideoPlayHint()`, `home.js`), and it listens to the real `play`/`pause` events on `#introVideoEl` to hide/reappear, so it also correctly disappears if the user starts playback via the native control bar directly rather than the hint. This is not a reversal of the earlier "native controls only" decision - the native `<video controls>` bar is untouched and still does all the actual control work; the hint is a thin visibility layer on top, scoped to the upload path only (YouTube's `<iframe>` already shows its own large red play button by default, so no hint is added there).

**Admin-configurable thumbnail (`home_video_thumbnail`).** Only meaningful for the upload path, since a `<video poster>` is what the thumbnail feeds - YouTube supplies its own thumbnail and has no external-poster hook. Lives in the "Video" tab now, not the generic "Hình ảnh" grid: it's edited alongside `video_mode`/`video_youtube_url`/`video_file` through the same form and `updateVideo()` action (`VideoSettingRequest` gained a `video_thumbnail` image rule; the controller stores/deletes it independently of which `video_mode` is active, so switching modes doesn't lose a previously-uploaded thumbnail). This was also the real fix for the play button reading as "alone in a void" one redesign earlier: an admin with nothing configured saw a lone circle floating in a flat `#232323` rectangle with nothing to anchor it - now that same empty rectangle is filled by a real poster frame from the moment the page loads, no JS or click required.

**Redesign 5 - the text hierarchy's roles, swapped.** Every redesign up to this point kept "Học mà chơi, chơi mà học." (the club's philosophy tagline) as the large, primary text and "Một ngày học tại Alpha Kids" as a small 11px uppercase eyebrow label above it. The user asked for the opposite, and the reasoning holds up on inspection, not just preference: "Một ngày học tại Alpha Kids" states plainly what the section/video is about - it's doing a heading's actual job - while the tagline is a mood line better suited to a supporting role underneath a clear heading than to carrying the section alone. `.intro-video__eyebrow` and `.intro-video__statement` (the bespoke classes this section had carried through 4 straight redesigns, the one place on the page with its own one-off text treatment) are gone. In their place: a plain `<h2><span class="hl">…</span></h2>` + `<p>`, the exact structure every other section-head on the page already uses - `.intro-video__lead h2` and `.intro-video__lead p` are written to literally match `.hp-section--ink .section-head h2`/`p`'s values (`clamp(2.2rem,3.6vw,3.2rem)`/800 weight for the heading, `16px`/`rgba(255,255,255,.7)` for the subtext) rather than a new scale, and `.intro-video__lead .hl` matches `.hp-section--ink .hl`'s `--accent` override. The section still doesn't carry the `hp-section--ink` class itself (its background is the partial split-overlay, not a full ink fill), so these values are restated locally rather than inherited - but restated as exact matches, not near-misses, so the section finally reads as *the same kind of heading* as Club Intro, C.A.R.E, and the pillars journey instead of a one-off.

### Manifesto List (C.A.R.E)
Full-width stacked rows on the `ink` dark band, each a giant `--accent` (bright gold, safe on dark) letter beside its name and Vietnamese gloss, divided by hairlines. Even rows carry a small `translateX` offset resolving to 0 on scroll-reveal. **Photo rail:** when Admin uploads `home_care_photo_1`/`_2`, the section becomes a 2-column layout (`:has(.care__photos)`), list on the left, a sticky stacked photo rail on the right filling what was previously bare `ink` background on wide screens; with no photos it stays the original centered 1-column list. Never both column layouts fighting for the same width - the `:has()` selector switches cleanly between them.

### Horizontal Carousel (class tiers, activities)
`display:flex; overflow-x:auto; scroll-snap-type:x proximity`, cards sized to intentionally show a "peek" of the next card on mobile. Native scroll, no JS scroll-hijacking. Each carousel ends in a non-item tile that fills leftover row width on wide screens: `.tier-card--consult` (a dark "chưa chắc chọn lớp nào?" CTA card) and `.activity-card--more` (a dashed "xem tất cả" link card) - both are real navigation, not decorative filler. Class tier cards also carry a 3-point `.tier-card__focus` bullet list grounded in the confirmed 4-tư-duy/C.A.R.E framework, replacing the old single generic sentence.

### Flexible Proof Grid (Vì sao phụ huynh chọn Alpha Kids)
Replaces the old bare testimonials placeholder. Backed by the `ProofPoint` model (Admin CRUD at "Vì sao chọn Alpha Kids"): `title`, `description`, optional `author_name`/`author_role`, optional `icon`, `sort_order`, `is_active`. Rendering branches on one field - `ProofPoint::isTestimonial()` (true when `author_name` is filled): a plain point renders as a compact icon+title+description card (`.proof-card`); a testimonial renders as a wider quote card (`.proof-card--quote`, `flex-basis: 380px` vs. 260px) with a large gold quote glyph, dark ground, and attribution - the same `flex-wrap` grid holds both without a template change. Ships seeded with 4 confirmed-fact proof points (multi-branch, C.A.R.E, age-tiered curriculum, free-trial policy) so the section is never empty; Admin replaces them with real parent testimonials over time by editing the same records and filling in a name.

### Accordion (Q&A) - shared primitive
One implementation (`base.css` + `initAccordions()` in `app.js`, loaded site-wide) backs both the full `/cau-hoi-thuong-gap` page and the homepage's "Còn thắc mắc về Alpha Kids?" section - same markup contract (`[data-accordion]` wrapping `.accordion-item` rows, each a `button[data-accordion-trigger]` + sibling `[data-accordion-panel]`), same motion. Bung/thu uses `grid-template-rows: 0fr → 1fr` (no JS height measurement); the trigger icon is a plain CSS-drawn plus/minus (two pseudo-element bars, the vertical one rotates 90° into the horizontal one - never a chevron sprite, no icon font). Each `[data-accordion]` container allows exactly one open item at a time (`initAccordions()` closes siblings on click); the first item renders `aria-expanded="true"` server-side on the homepage teaser so that block is never empty on load. **The full FAQ page is the one exception (2026-08-18, explicit user request): every item renders `aria-expanded="false"`, none open by default.** `initAccordions()` itself has no dependency on an initial open state (pure click-toggle, `grid-template-rows: 0fr` is already the correct closed-state render), so this was a one-line change local to `faq.blade.php` - no JS/CSS change, and the homepage teaser's own first-item-open behavior is untouched. `.accordion--boxed` is the only modifier in use: each question becomes its own bordered, rounded card (`--radius-lg`, hairline border - Hairline-Over-Shadow Rule, no shadow) with spacing between cards instead of a flush hairline-divided list; the open card's background switches to `--accent-soft` via `:has(.accordion-item__trigger[aria-expanded="true"])` - pure CSS, no extra JS class. Backed by a single `Faq` model (Admin CRUD at "Câu hỏi thường gặp"): `question`, `answer`, `sort_order`, `is_active`, `show_on_home` - `show_on_home` controls whether a question also surfaces on the homepage section, independent of its "Hiển thị" (`is_active`) status on the full FAQ page.

### Contact FAB - new shared primitive, site-wide (2026-08-18)
A fixed bottom-right speed-dial bubble, included once in `layouts.client.blade.php` (`partials.client._contact-fab`) so it renders on every client page rather than being built per-page. Origin: the user supplied a screenshot of the generic Vietnamese-small-business pattern (three permanently-stacked, brand-colored circles - Zalo blue, Messenger gradient, phone green) and asked whether it made sense site-wide. Rebuilt, not cloned: three different external brand colors sitting fixed on every page is a direct violation of The Color Consistency Lock, the exact defect the v3 palette rebuild exists to prevent. Reduced to one accent language:
- **Closed state:** a single `--accent` (gold) circle, `--ink` chat-bubble icon, `56px`, `--shadow-md`.
- **Open state (click-toggle, not hover - mobile has no hover):** two `--white` circles fade/scale up above it (`.contact-fab__option`, `48px`, hairline border, `--accent-text` content, same `--shadow-md`), the trigger's icon crossfades from chat-bubble to an X. `initContactFab()` in `app.js` (site-wide script) handles the toggle, closes on outside click and `Escape`.
- **Only two real, working channels - not three.** The screenshot's third bubble (Messenger) has no backing `Setting` anywhere in the project (`messenger_url` doesn't exist) - shipping it would be a dead or fake button, which the site's own honest-empty-state rule (Do's list, "never fabricated") argues directly against. Scoped to `zalo_url` and `hotline`, both already-configured `Setting` keys; the whole partial renders nothing (`@if ($fabZaloUrl || $fabHotline)`) if neither is set, rather than showing empty bubbles.
- **Zalo bubble is text, not a logo mark - following the site's own existing precedent.** `_footer.blade.php`'s social row already renders Zalo as a plain bold-text pill (`.is-text`, no icon) for the identical reason: no clean single-color Zalo glyph fits a one-accent-hue system. The FAB's Zalo option reuses that exact solution (`.contact-fab__option-text`, "Zalo" in `--font-display` 800) rather than importing a brand icon.
- **Superseded the in-page contact nudge added earlier the same day.** `registration/create.blade.php`'s `.form-card__nudge` ("Muốn nhanh hơn? Gọi hoặc Zalo …") was cut once this existed - a page-local, always-static nudge became redundant next to a persistent, site-wide, always-reachable equivalent. `Client\RegistrationController@create` no longer builds a `$contact` array; the FAB reads `$siteSettings` directly (available for free since it's included from `layouts.client.blade.php` itself, not a child `@section`, so the existing `View::composer('layouts.client', SiteSettingsComposer::class)` binding covers it with no new plumbing).
- **z-index 90** - above ordinary page content, below the sticky header (`100`) and mobile-menu overlay (`200`), and well below the success modal (`999`, see below, no collision anyway since the two never show at once).

### Success Modal - replaces the corner flash-toast (2026-08-18)
`session('success')` is client-side-only and, in practice, exclusively the registration confirmation (`Client\RegistrationController::store()`/`quickStore()` are the only two call sites) - so this is honestly "the registration success moment," not a generic system toast, and was designed as one rather than a corner notification. Replaced the old `.flash-toast` (top-right, `--color-success` green, 4s auto-hide, `initFlashToast()`) with `partials.client._success-modal`, a centered overlay: `.success-modal__backdrop` (`rgba(16,16,16,.75)`) behind a white `--radius-xl` card that scales+fades in (`cubic-bezier(.16,1,.3,1)`, matching the site's established entrance easing), a gold `--accent` circle badge whose checkmark path draws itself in via `stroke-dasharray`/`stroke-dashoffset` on a delay *after* the card lands (not simultaneously - the sequencing is the part that reads as a considered moment rather than everything arriving at once), and a soft `--accent-soft` halo that blooms in behind the badge a beat after that.
- **Gold, not the pre-existing `--color-success` green - a deliberate departure, not an oversight.** `--color-success: #1E8E5A` predates this pass and was never part of the Ink/Gold/Paper system; reusing it here would have put a second, unrelated accent hue on the single most emotionally significant moment on the site (a completed registration). The badge uses the same `--accent` fill / `--ink` content pairing as `.care-detail-card__letter` and other solid-gold badges elsewhere, so the celebration reads as *this brand's* gold, not a borrowed generic-SaaS green. `--color-success` itself is left defined (it pairs with `--color-danger`, which is genuinely in use) but nothing currently reads it.
- **No auto-dismiss.** The old toast vanished after 4s unprompted; a centered modal that silently disappears while someone is still reading it is a worse failure mode than a toast doing the same in a corner. Closes only on the "Đã hiểu" button, the × button, backdrop click, or `Escape` - all wired in `initSuccessModal()` (`app.js`), which replaces the deleted `initFlashToast()`.
- **Found and fixed a real duplicate token while wiring this up.** `.field-error` (added earlier the same day for the registration form) was pointed at a new `--danger: #C0392B` - but `--color-danger: #C0392B`, identical value, already existed in `:root` and was simply missed. Removed the duplicate `--danger` token and repointed `.field-error` to the pre-existing `--color-danger`.

### Navigation
Uppercase, bold, small (11.5px) labels; active item gets a gold underline. Transparent (home hero) header variant renders white text for legibility against the scrim; the solid variant uses ink on the light background.

**Transparent header - fixed with its own scrim, not absolute riding on the hero's.** Two real, verified problems in the original implementation, not aesthetic guesses: (1) `.site-header--transparent` was `position: absolute`, which scrolls away with the hero - a Playwright check confirmed the header fully off-screen (`top: -1200px`) after scrolling one viewport, meaning the entire rest of the (long) homepage had zero navigation, no way back to any page or the CTA without scrolling back to the very top; (2) the header's white nav text had no contrast guarantee of its own - it was riding on `.hero__photo-overlay`, a scrim shaped for the hero's *own* text zone (bottom-left), which deliberately leaves the top-right unshaded for the gold mesh glow - exactly where four of seven nav items and the CTA sit, so legibility depended on the luck of whatever the hero photo's top-right corner happened to show. Fixed by giving `.site-header--transparent` `position: fixed` (stays pinned through the whole scroll, same as `--solid` already did via `sticky`) plus its own `::before` gradient scrim (180px, independent of the hero's), and adding an `.is-scrolled` state - toggled by `initHeaderScroll()` in `app.js` (one rAF-throttled `scroll` listener, `window.scrollY > 48`, site-wide script since the header partial is shared) - that switches the header to the exact same look `--solid` already uses (`--color-bg` background, `--shadow-sm`, ink text), not a third visual language. The interior pages' `--solid` variant (`position: sticky`) was left untouched - it already had neither problem. Verified end-to-end on the live site: nav stays legible against a deliberately bright sky patch stress-tested in the Artifact preview first, and the header is still pinned and readable four sections deep into the homepage (checked at the C.A.R.E section).

### Signature Component: Sticky-Scroll Timeline
Four full-width panels each `position: sticky` at the same header-offset `top`, later panels covering earlier ones as the user scrolls (pure CSS for the pin). `home.js` computes real scroll progress via one rAF-throttled listener, writing a `--progress` custom property (set on `#timelineStack` so descendants inherit it) that drives a vertical rail's `scaleY` fill; each panel gets `is-active` (numeral brightens to `--accent-text`) once its `getBoundingClientRect().top` crosses an activation line set at `1/3` of viewport height - i.e. the instant the panel scrolls into the bottom two-thirds of the viewport, well before it visually pins at the header offset. This threshold moved earlier through three rounds of user feedback (originally a global-linear progress across the whole 4-panel stack, then panel-pin-position, then 2/3 viewport, settled at 1/3 viewport) because the rail visually lagging behind the panel the user was already reading felt broken. The rail fill transitions (`transform .5s`) between the four discrete steps rather than snapping, since progress no longer changes continuously.

**Active-state enrichment.** Five devices carry the panel from a bare numeral+text into something with real information depth, all cross-referencing content already established in the Pillars section above it rather than inventing anything new: (1) each panel's icon is the exact SVG used by its matching pillar tile, dim by default and lifting to an `--accent-soft` roundel when active; (2) a 2px underline draws in from `width: 0` to `64px` on activation (`transition: width`), a "being written" cue rather than a flat color swap; (3) each panel carries its pillar's C.A.R.E cross-reference tag (e.g. "Ask & Answer"); (4) a 4-dot progress cluster (`#timelineDots`) sits `position: sticky; top: 50vh` on the rail throughout the whole scroll range, filling gold in step with the same `activeCount` the JS already computes for panel `is-active` - no separate calculation, hidden below 768px to avoid rail crowding on narrow screens; (5) an anchored photo column (`.timeline-panel__media`) on the right, described below.

**Anchored photo column.** The right side of each panel originally carried only a small (220×220px) square photo pushed to the far edge by `margin-left: auto`, leaving a wide dead gap next to the (`max-width: 34ch`) text column - read as empty rather than intentional. Redesigned right-side-only (left side - number, icon, heading, underline, paragraph, tag - deliberately untouched, per explicit instruction) after a written Interactive Design Pitch and a Claude Artifact preview approved by the user: the photo is now wrapped in `.timeline-panel__media`, resized to a portrait `280px` / `aspect-ratio: 4/5` `.timeline-panel__visual` with a `1px solid var(--ink-faint)` hairline border (matching every other framed photo on the site) that shifts to `var(--accent)` on `.is-active`, plus a small `.timeline-panel__folio` caption ("01 / 04" through "04 / 04", reusing the site's editorial-numbering convention) that shifts to `--accent-text` on `.is-active` - giving the photo the same activation-state color response the icon, underline, and numeral already had. Falls back to the `:not(:has(.timeline-panel__media))` selector (widening the paragraph to `60ch`) when no Admin photo is configured for that panel, and hides below 1100px alongside the rest of the media query.

### Signature Component: Hero Mesh Gradient
The hero's scrim layer is not a flat linear gradient: it layers two radial gradients (a soft gold bloom at the top-right, away from the text zone; a strong black radial anchored bottom-left, under the text) under the base linear gradient, then drifts the whole composite a few percent over 22 seconds (`background-position` animation, ease-in-out, alternating) to suggest stadium floodlight movement. Disabled entirely under `prefers-reduced-motion`. Contrast for the text zone is verified independent of the drift - the darkest stop never moves into the text's safe zone in a way that would reduce contrast below the mesh's resting state.

### Signature Component: Closing CTA - dark full-bleed statement, shared across pages
Corrected against the actual current implementation (this entry previously described an earlier gold-background iteration that no longer matches the code - see history below). `.closing-cta` is a full-bleed `100vw` breakout (`margin-left: calc(50% - 50vw)`), `--ink` background, an optional Admin-uploaded `.closing-cta__photo` under a flat `rgba(16,16,16,.637)` `.closing-cta__scrim`, white headline (`display-letter` type step, `clamp(2.8rem,5.4vw,4.4rem)`), gold-accented span via `.text-accent-free`, and a gold `.btn--accent` primary action. Never depends on the photo being configured - falls back to the flat `--ink` field, so it can never render half-finished. Moved from `home.css` into `base.css` (with `.text-accent-free` and `.closing-cta__actions`, a new class for a 2-button layout) when the About page needed the exact same component with different copy and a second (`.btn--outline-invert`) button - it is now a shared, site-wide primitive rather than a homepage-only one. (A prior pass documented here described a solid-gold-background version with `--ink` text, rejected at the time as "Glass-on-Mesh" for repeating the Hero's dark/photo/scrim recipe - the code found in this pass no longer matches that description; corrected to reflect what actually ships today rather than guess at the undocumented intermediate change.)

### Hero (3-layer contract, max-4-element stack)
Exactly 3 layers: (1) full-bleed photo, `min-height: 100dvh`, with scroll-linked parallax; (2) the mesh-gradient scrim described above; (3) text/CTA, fade+slide-in on page load. The text layer holds at most 4 elements (eyebrow mark, headline, subtext, CTA row) - a feature checklist does not belong in the hero; it lives in the highlight strip directly below.

### Highlight Strip
A thin `ink` band directly under the hero carrying the former hero checklist as 4 short phrases in one row (2×2 on mobile), divided by hairlines, no icons, no bullets, no card chrome.

## Page: Về CLB (About)
First non-homepage page built out (previously a stub: bare `.section` + `<h1>`). Content came from a reference mockup the user supplied - carried over verbatim (headings, copy, the 5 core values, the 6 stat labels), explicitly *not* its purple-card visual language; every section is built from primitives already established on the homepage rather than a new visual world, per an approved Interactive Design Pitch + Artifact preview (iterated once: the banner was first built as a 2-column text/photo split, then corrected to be a literal clone of `.faq-hero`'s size and centered layout with a wide photo band below it instead, per explicit feedback that it should match the FAQ page's banner "y đúc" - exactly).

- **Banner (`.page-hero`)** - shared, full-bleed (`100vw` / `margin-left: calc(50% - 50vw)`, same breakout as `.closing-cta`) section used by every non-homepage page. What started as `.about-hero` and `.faq-hero` - two separate declarations with identical values - was consolidated into this one class in `base.css` (moved out of `faq.css`/`about.css`). The homepage's own Hero is intentionally excluded - full-bleed 3-layer photo/scrim/text at `100dvh`, a different, larger component this one doesn't try to replicate.
  - **Two states, one class.** No Admin photo configured (FAQ today) → flat `--paper-alt`, `--ink` text, exactly the original `.faq-hero` look. Photo configured (`.page-hero--photo` modifier, About today) → `.page-hero__photo` (`position:absolute; inset:0`, `object-fit:cover`) fills the section as a background, `.page-hero__scrim` (`rgba(16,16,16,.6)`, flat) sits above it, and the text (`.container`, `z-index:2`) sits above both - photo → scrim → text, matching the same layer order as the Hero and Closing CTA rather than inventing a fourth pattern. Text flips to white/`--accent` under `--photo` (Two-Gold Rule for the `.hl` span).
  - **Banner photo went through three shapes before this one** - each a real, distinct mistake, not taste-polishing: (1) a *contained*-width `21/8` box placed **after** the text (not behind it) - at ~1216px container width that rendered ~490px tall, nearly tripling the banner's height versus FAQ's; reversed as not "y đúc." (2) A corrected-height full-bleed version, still placed **after** the text as a separate stacked block below it (fixed `height: clamp(220px, 30vw, 380px)` instead of a width-driven aspect-ratio - a real fix, kept in the final version) - but the photo sitting *after* the copy rather than *behind* it as its backdrop was itself the actual, different request; reversed again. (3) Current: same fixed-height photo, but layered as background + scrim + overlaid text within the same `.page-hero`, no longer a second stacked block.
  - **Shared going forward, not About-only.** `method`, `program`, `activity`, `branch`, `parent`, and `registration` already carry their own `{page}_banner` key in `OtherPagesImageGroups` (added before this page existed, unused until now) - each can adopt `.page-hero` + `.page-hero--photo` with zero new CSS when built out.
- **Giới thiệu, gộp vào Tầm nhìn/Sứ mệnh.** Originally its own `.section` - a heading plus one paragraph floating alone in a large white section, nothing else anchoring it, sitting right below a banner making a near-identical claim. Reported back as reading unfinished, not just under-styled. Fixed by merging it into the Tầm nhìn/Sứ mệnh section as that section's `.section-head--center` lead-in, on the same `--paper-alt` ground - one chapter ("who Alpha Kids is / what it believes") instead of two thin, disconnected scroll-stops.
- **Tầm nhìn / Sứ mệnh** - 2 bordered cards (hairline, no shadow - Hairline-Over-Shadow Rule), each an icon roundel (`--accent-soft` fill, `--accent-text` stroke - the light-ground-safe pairing) + heading + a drawn `.value-pair__rule` + copy. Content is fixed (not Admin-editable), matching the precedent already set by the homepage's C.A.R.E principles, which are hardcoded in `home.blade.php` rather than database-driven.
- **Giá trị cốt lõi - replaced outright by "Mô hình 4 tư duy," 2026-08-18.** Went through two prior redesigns (a `.core-values` 5-across icon grid, then a `.value-rows`/`.value-row` 2-column list with a spanning last-row orphan fix), then a further unrecorded pass to `.value-bento` (3-col grid, one `--featured` cell spanning 2 columns, one `--alt` cell) - this entry is corrected to that as the last state before removal, since the `.value-rows` description above had drifted from the code. Cut per explicit client instruction, applying the same design-taste-frontend reasoning used to cut Method's heart-banner: the 5 core values (Kỷ luật/Tôn trọng/Nỗ lực/Đoàn kết/Tiến bộ) no longer appear anywhere on the site after this and Method's heart-banner removal earlier the same pass - a real content loss, flagged here rather than silently absorbed, and the section's own layout slot is now filled by Method's relocated "Mô hình 4 tư duy" `.pillar-bento` (ghost-numeral scroll-ignite, connecting SVG line, 4 cards: Giao tiếp/Đánh giá/Ứng xử/Lãnh đạo) instead of standing empty. No Section-Layout-Repetition conflict: About held exactly one bento-family section before (`.value-bento`) and holds exactly one after (`.pillar-bento`), same slot, same rhythm (`.section--padding-block-80`, plain `--paper` ground between the two `--alt` sections either side). `.pillar-bento*`/`.pillar-card*` CSS and `initPillarBento()` JS (new `about.js`, About's first page-specific script) moved wholesale from `method.css`/`method.js`, which is now deleted (had no other export). The page's own meta description (`about.blade.php`) was updated to drop "giá trị cốt lõi" and name the new section instead, so the SERP snippet doesn't promise content the page no longer has.
- **Đôi lời tâm sự - replaced the stat block entirely, not just restyled it.** The original "Alpha Kids hiện nay" stat block (photo + 2×3 number tiles) sat directly after the icon-heavy "Giá trị cốt lõi" bento grid - two consecutive "structured tile" sections back to back, both reading as data blocks. Client feedback asked for something personal instead: a short first-person message from a coach representing the club. Kept `.section--alt` (unchanged rhythm - Closing CTA remains the page's one `--ink` section). No card chrome (no border, no icon roundel) as a deliberate contrast against the bordered-card language used everywhere else on this page: after a grid of 5 bordered tiles, the page needed a moment without boxes before the bold `--ink` Closing CTA. Typography leans on a classic drop cap (`::first-letter` on the opening paragraph, sized in the existing "Display accents" range) instead of a quote-glyph, since the homepage's own Proof section already owns the quote-glyph motif - reusing it here would read as the same component copy-pasted across pages. Closes with `.letter-body__rule`, the exact `.value-pair__rule` accent-rule treatment from the Tầm nhìn/Sứ mệnh cards above it, reused rather than inventing a new "how a block ends" device. Content is 3 `Setting` key/values (`about_letter_name`, `about_letter_role`, `about_letter_message`) editable from the "Về CLB" tab in Cài đặt → Nội dung trang khác, plus `about_letter_photo` in `OtherPagesImageGroups`; no message configured → honest dashed-hairline empty state (`.letter-empty`), never a fabricated quote (Do's rule). `about.js` and its `initCountUp()` were removed outright, not just unused in markup - the stat block was its only caller.
  - **Wall-of-text fix, redesigned four times (2026-08-19).** With no character-count guidance in the admin form, `about_letter_message` (max 2000 chars) routinely fills with several long paragraphs. **Pass 1** kept the original photo-beside-text grid (fixed 4:5 portrait, fixed-height column) and only patched it (sticky photo + a dimmed closing line) - rejected on sight ("xấu," "bố cục khác đi"): the photo was built for a short quote, patching spacing/color around it doesn't fix a layout whose premise doesn't fit long text. **Pass 2** shrank the photo to a small 52px circular byline above the text, with the body switching to a 2-column newspaper flow past 420 characters - also rejected ("ảnh phải to," "bố cục khác"): correct instinct (remove the height mismatch) but the wrong trade, since a big photo is a stated requirement for this section. **Pass 3** reused the homepage's Club Intro split ratio (`grid-template-columns: minmax(0,5fr) minmax(0,7fr)`) with the photo column set to `align-items: stretch` so it grew to match the text column's exact height - correct on paper, but nobody had actually looked at the rendered result against the real uploaded photo. **Pass 4 (current) found the real defect by screenshotting the live page** (`npx playwright screenshot`, since no in-session browser tool was available) instead of reasoning from CSS alone: `about_letter_photo` is a 1920×1048 (~16:9) landscape action shot, not a portrait. Stretching it to match a tall, Admin-editable text column forced `object-fit: cover` into an extreme vertical sliver - the actual render showed only a shoe, a ball, and grass, no sense of "a real coach," which is what read as "sao sao á" despite the CSS being technically correct. **The fix: stop coupling the photo's shape to the text's length at all.** `.letter-block` is now a simple stacked flow, not a grid - a full-width `.letter-visual__frame` at a fixed `aspect-ratio: 16/9` (matching, not fighting, the asset's native ratio) sits on top, `.letter-visual__caption` (name · role, a plain inline byline) directly under it, then `.letter-body__text` (capped at `640px` for a comfortable measure) flows below at its own natural length - short or long, the photo is always the same generously-sized shape because its height no longer depends on the text at all. Verified this time by screenshotting the actual rendered page at both desktop (1440px) and mobile (390px) viewports before calling it done, not just reading the CSS. Name/role caption, drop cap, and `.letter-body__rule` closer carried over from Pass 3 unchanged.
  - **The content itself was the other half of the problem, found after Pass 3 still read "sao sao á."** The live `about_letter_message` (client-supplied, drafted via ChatGPT, never audited) was a textbook case of the same AI-writing tells this project's `seo-audit` skill already corrected on Method's C.A.R.E cards (see that page's entry): the "không chỉ X, mà còn Y" template used 3 times in 10 short paragraphs, two chained 5-6 item abstract-noun lists ("hợp tác, kiểm soát cảm xúc, kiên trì, suy nghĩ trước khi hành động và tự tin hơn" / "vận động, suy nghĩ, trải nghiệm, mắc sai lầm, vượt qua thử thách và trưởng thành"), and a 3-sentence staccato tricolon closer ("bóng đá chưa bao giờ chỉ là bóng đá" / "Đó là một cách để giáo dục trẻ" / "Và đó cũng là lý do Alpha Kids được bắt đầu") - a layout can only distribute a wall of generic-sounding text more gracefully, it can't make it stop reading as generic. Rewritten with the same checklist, length-parity not a trim (1249 chars vs. the original's similar length, still comfortably under the 2000-char field limit): kept every concrete, real detail (5 years in a Japanese football environment in Thành phố Hồ Chí Minh, returning to his hometown to found Alpha Kids) and every actual sentiment, cut the repeated template to a single natural use, replaced the abstract-noun chains with 2-3 concrete beats each, and collapsed the tricolon closer into two plain sentences. Written directly into the `Setting` row (`about_letter_message`, via `php artisan tinker`) rather than the blade or a seeder, since this is genuinely Admin-owned content, not page markup.
  - **Pass 5 (current, shipped) - float, not a grid at all, plus a fan-reveal.** Pass 4's stacked full-width photo genuinely fixed the dead-space problem, but the client then asked for two more things a stacked layout can't give: the photo living beside the opening paragraphs rather than only above them, and a decorative reveal (two dimmed duplicates of the photo fanning out from behind the main frame on scroll-in, asymmetric on purpose - one sheet spreads further and lifts, the other spreads less and settles, staggered `0.24s` apart, `letter-visual__ghost--a`/`--b` in `about.css`, `initLetterFan()` in `about.js`). Reconciling "beside the text" with "never any dead space regardless of letter length" (the Extremes check from `impeccable/layout.md` - "does long content expose a structural failure?", which every grid/flex column pairing tried in Passes 1-3 fails at some length) landed on plain CSS `float: left` on `.letter-visual` (`width: 400px`) - the standard html-since-CSS1 answer to exactly this brief: a fixed-size image the surrounding paragraphs wrap beside for as long as it's tall, then automatically reclaim the full column width the instant they clear its bottom edge (`.letter-block::after { clear: both }`). No JS measurement, no sticky, no height-matching, correct at 1 paragraph or 10. `.letter-visual__frame` is `aspect-ratio: 4/5` (portrait, explicit client request over the wider crop Pass 4 shipped) - a taller ratio crops tighter into the wide 1920x1048 source than 16/9 does, accepted as the deliberate trade for the "big, present, portrait-leaning photo" the client kept asking for through every pass. Verified on the real page (not a preview) at 1440px and 390px via a scripted Playwright screenshot before shipping; `detect.mjs --scope layout` clean on all three changed files. Below the `700px` breakpoint (matching this file's other mobile rules) `.letter-visual` drops the float entirely (`float: none; width: 100%`) and stacks above the text, since there's no room left for anything to wrap beside.
  - **Two bugs found on the real page, not the preview, and fixed the same day.** (1) The fanned ghosts painted over the wrapped paragraph text on the left edge - `.letter-visual__stack` was missing the `padding: 0 9%` the preview version had, so `.letter-visual__ghost` (then `inset: 0`) filled the float's full padding box instead of matching `.letter-visual__frame`'s narrower content-box width, and the fan's `translateX` carried it straight into the text column. Fixed by restoring the padding and switching the ghosts to explicit `top/bottom: 0; left/right: 9%` (matching the frame's actual box) instead of `inset: 0`. (2) `.letter-visual__frame`'s aspect ratio went `4/5 → 5/6` per explicit client request, a touch shorter without giving up the portrait lean.
  - **The two ghost photos became real Admin-configurable images, not a crop trick on one photo.** Originally both fanned sheets reused `about_letter_photo` at different `object-position` values purely to demo the effect - a real limitation flagged at the time. Added `about_letter_photo_2`/`about_letter_photo_3` to `OtherPagesImageGroups`' "Đôi lời tâm sự" group (`app/Support/OtherPagesImageGroups.php`) - the existing image-group system is fully config-driven (the admin form, `updateImages()`, and `otherPages()`'s `$images` array all read this array generically), so this needed zero controller or view-generation changes, only the two new array entries plus threading the two new `Setting::get()` calls through `AboutController` and `about.blade.php`. Each ghost falls back to `about_letter_photo` when its own key is unset (`$images['about_letter_photo_2'] ?? $images['about_letter_photo']`), so the effect never breaks or shows a blank sheet before Admin uploads the two extras - same honest-fallback shape as the mobile hero banner reusing the desktop photo.
- **Motion, added in this pass.** `data-reveal-group` + `.reveal`/`.reveal-d1`..`.reveal-d5` on every section (fade+slide, staggered) - the same mechanism the homepage already had, but `initRevealOnScroll()` lived only in `home.js` until now. Moved to `app.js` (site-wide) since About needed it too; `.reveal`/`.is-inview`/`.reveal-d1`–`.reveal-d4` moved from `home.css` to `base.css` alongside it (added `.reveal-d5` for the 5-item value list), `home.js`'s own call and definition removed to avoid running the observer twice. Stat numbers count up from 0 on scroll-into-view (`about.js`, `initCountUp()`, easeOutExpo, fires once per page load). Neither this nor the reveal system gates on `prefers-reduced-motion` - a standing site-wide rule (the user's own OS has it set to reduce, and accessibility-respecting fallbacks on this project have repeatedly read as "broken," not as an accommodation).
- **Closing CTA** - the shared `.closing-cta` component (see Signature Components), reused verbatim with page-specific copy and a second button (`.btn--outline-invert`, already existed for dark-ground secondary actions - see Hero). Now the page's only `--ink` section. Gained an optional Admin background photo (`about_closing_cta_photo`, new "CTA cuối trang" group in `OtherPagesImageGroups`) mirroring the homepage's `home_closing_cta_photo` exactly - same `.closing-cta__photo` (`position:absolute; inset:0; object-fit:cover`) → `.closing-cta__scrim` (flat `rgba(16,16,16,.635)`) → text layer order already defined in `base.css`, so no new CSS was needed, only the `<img>` conditional in the blade and the Setting plumbing. No photo configured → falls back to the flat `--ink` field exactly as before.
- **Shared-primitive extraction.** Building this page surfaced that `.section-head`, `.section-head--center`, `.hl`, and `.closing-cta` (+ children, `.text-accent-free`) were homepage-only by accident of file location, not by design - they carried no actual homepage-specific logic. Moved to `base.css` as `.section`/`.section--alt`/`.section--ink` + the above (home.css keeps only its own `.hp-section` wrapper and the `.hp-section--ink` overrides for `.hl`/`.section-head`, which still apply since they target the same now-shared class names). About's own `about.css` holds only what's genuinely page-specific: `.value-pair`, `.value-rows`/`.value-row`, `.letter-block`/`.letter-empty` and children.

## Page: Phương pháp (Method)

Second non-homepage page built out from a stub. Content and structure came from a reference mockup the user supplied (banner, 2-card intro, 4-card C.A.R.E breakdown, 4-step pillar summary, a photo+values closing banner, CTA), carried over verbatim - explicitly *not* its purple-gradient, per-letter-rainbow visual language, following the same approach set by the About page: build from primitives already established, single accent hue throughout.

- **Banner, C.A.R.E cards, and closing banner reuse `OtherPagesImageGroups` keys that already existed** (`method_banner`, `method_care_celebrate`/`ask_answer`/`repetition`/`enjoy_strict`, `method_heart_banner`) before this page had any real content - added ahead of time when the About page's image-group scaffolding was built out. All copy is fixed in the blade, not Admin-editable, matching the precedent already set for Tầm nhìn/Sứ mệnh and C.A.R.E on other pages.
- **`.value-pair`/`.icon-roundel`/`.value-pair__rule` moved from `about.css` to `base.css`.** Reusing `.value-pair` for the "Nền tảng phương pháp" cards assumed it was already a shared primitive like `.page-hero`/`.closing-cta` - it wasn't; it had only ever lived in `about.css`, page-scoped. Method doesn't load `about.css`, so `.icon-roundel svg` had zero CSS applied on this page, and the bare `<svg>` fell back to the browser's default replaced-element size (300×150), rendering as a giant icon overlapping the card text. Moved the whole block to `base.css` (About's `.value-card`/`.value-bento` overrides of `.icon-roundel` for the featured/alt variants stay in `about.css`, unaffected - specificity still wins regardless of which file a rule lives in). The same class of bug the earlier "Shared-primitive extraction" pass on About was meant to prevent going forward; missed here because the reuse wasn't re-verified against which file the class actually lived in before shipping.
- **Banner** - `.page-hero`/`.page-hero--photo`, reused verbatim from About/FAQ, new copy only.
- **"Nền tảng phương pháp" (2 cards)** - reuses `.value-pair` verbatim (same component as Tầm nhìn/Sứ mệnh on About).
- **"Phương pháp huấn luyện C.A.R.E" (4 detail cards) - new `.care-detail-grid`, not a reskin of the homepage's C.A.R.E list.** The homepage's `.care-row` is a bare list (letter + heading, no photo); this page needed more depth (subtitle, longer copy, a representative photo per letter), so it's a genuinely different component rather than the same one restyled. Letter badge (`.care-detail-card__letter`) is a single flat `--accent` circle, not the mockup's red/blue/green/orange per-letter palette - one accent hue holds regardless of how many items are being distinguished (numbering/position does that job, not color). Card order is letter + heading + subtitle + description first, photo last - matching the mockup's own hierarchy (identity and explanation before the supporting photo); an earlier draft inverted this (photo first) and buried the letter identity below an anonymous image, corrected after review.
  - **Rebuilt 2026-08-18: grid `4 cols → 2 cols`, copy rewritten, internal typographic hierarchy added.** Original copy (client-supplied, drafted via ChatGPT) ran 115–148 words per card as one unbroken paragraph in a ~300px column at 13.5px/1.6 - measured at 11–14 visually unbroken lines per card, the exact "wall of text" complaint the client raised. Two designs were built, tested via Artifact preview, and rejected before this one shipped: (1) a full-width "editorial rows" treatment with a giant letter, a side-by-side photo, and a continuous connecting spine down the left margin - rejected on self-review because it converged on the homepage's own Manifesto List C.A.R.E component (full-width stacked rows, giant letter, photo rail) almost exactly, undermining the very reason this page has its own C.A.R.E component (see above); it also would have made the section 3–4x taller. (2) The card grid kept at 4 columns with only the internal copy re-hierarchized - rejected because it never fixed the actual column-width problem, only the wall-of-text inside it. Final: kept the page's own bordered-card language (`.care-detail-card`, unchanged shape/radius/photo-at-bottom), only widened it to 2 columns (`~24ch` column → `~46ch`), which gave enough room to keep the full rewritten copy without cutting it.
  - **Copy rewrite, not a trim.** The client's original ChatGPT-drafted text was rewritten for length-parity (115–148 words per card before, 125–138 after) using the project's own `seo-audit` skill's Content Optimization + AI Writing Detection checklist, not shortened for layout. Removed: an identical "not just X, it's also Y" contrastive template used as the closing line on all 4 cards (a literal AI-writing tell, not a style choice - repeating one rhetorical formula 4 times in a row is the tell, not the formula itself), chained 3–4 item abstract-noun lists ("tự tin, tinh thần tích cực và động lực"), and a stray filler "thực sự." Added: "Alpha Kids" named explicitly in every card (previously zero - the copy read as generic advice for any football academy, not this club's own coaching voice). Kept verbatim: ASK & ANSWER's 3 in-game coach questions (the copy's strongest concrete detail, now rendered as a real `<ul>` list instead of prose separated by `<br>`), and ENJOY & STRICT's closing tricolon - now the page's only card ending on a 3-beat parallel structure, since the other 3 no longer share that template.
  - **Internal hierarchy: `__lead` → `__text` (→ `__list` for Ask & Answer only) → `__punch`.** Each card's first sentence is pulled out as a bolder lead (`15.5px`/600). `.care-detail-card p` (bare-element selector) was replaced by an explicit `.care-detail-card__text` class specifically so `__lead`/`__punch` could carry different colors/weights without a specificity fight against a bare `p` rule.
  - **`__punch` color, corrected twice same day after direct user feedback.** First shipped as `--accent-text` (gold) directly on the card's `--paper` background with a short `--accent` rule above it - measured at ~5.2:1, technically AA-compliant, but a full 2-line sentence in bold gold read noticeably murkier than the same weight in `--ink` ("chữ hơi khó đọc," user's own words) - `--accent-text`'s documented uses elsewhere are all short (`.hl` spans, numerals, icons), not sentence-length body copy. First fix attempt moved gold into the fill role instead (`--accent-soft` background chip, `.icon-roundel`'s fill/stroke split reused for text) - rejected on sight ("xấu," user's own word): a solid tinted block nested inside an already-bordered card, at the card's own bottom edge, read as a boxed alert/notice rather than a quiet typographic accent, fighting the system's flat hairline-only emphasis language (no card in this system nests a second bordered/filled block inside itself). Final: kept the plain `--accent` `__punch-rule` (the same 2px bar already used by `.value-pair__rule`/`.letter-body__rule`) as the sole "this line is different" marker, and simply recolored `__punch` text to `--ink` - same high-contrast fix, zero new chrome.
  - **Alt text fixed as part of the same edit.** The `/impeccable critique` from 2026-08-13 (P3) had flagged all 4 photos as content-bearing but carrying `alt=""` - unlike this page's decorative background photos, these specifically show the method in action. Populated real alt text per letter while the markup was already open for this rewrite, rather than leaving it for a separate pass.
  - **Full-page follow-up pass (same day).** A whole-page review after the grid rebuild found the AI-writing-tell problem wasn't fully contained to the 4 detail cards - the section's own intro paragraph and the "Nền tảng phương pháp" card above it still carried the same "không chỉ X, mà còn Y" template and chained abstract-noun lists, missed in the first pass because that pass only touched the 4 cards themselves. Rewritten with the same `seo-audit` checklist. Also fixed: `.care-detail-card`'s heading was `<h4>`, skipping a level under the section's `<h2>` while the "Nền tảng phương pháp" cards directly above it use `<h3>` for the same kind of card heading - changed to `<h3>` (blade + `method.css`) for consistent depth. And: the "Mô hình 4 tư duy" intro card named a concept with no page-local detail section since that content moved to About (see above) and zero link pointing there - page had exactly one internal link total (the closing CTA). Added `<a href="{{ route('about') }}#pillarBento">Xem chi tiết tại trang Về CLB</a>` inline in that card, since `#pillarBento` is the real `id` on About's relocated component.
- **"Mô hình 4 tư duy" (4-step summary) - moved to the About page, 2026-08-18.** Was a `.pillar-bento` static reference here (ghost-numeral scroll-ignite, ghost `01`-`04`, connecting SVG line to each card's real measured center) - the same 4 pillars as the homepage's scroll-driven Timeline (Giao tiếp/Đánh giá/Ứng xử/Lãnh đạo), but a quick static echo, not a competing full experience. Relocated in full (blade markup, `.pillar-bento*`/`.pillar-card*` CSS, `initPillarBento()` JS) to replace About's "Giá trị cốt lõi" section - see that page's entry below for why. C.A.R.E now leads straight into the Closing CTA. The 2-card "Nền tảng phương pháp" intro above still name-checks "Mô hình 4 tư duy" as one of the method's 2 foundations; it no longer has a same-page detail section to back it up (the full treatment now lives on About, the scroll journey on Home), a deliberate trade against a third repeat of the same 4 pillars rather than an oversight.
- **"Giáo dục bằng trái tim, huấn luyện bằng tri thức" - removed.** Originally kept by explicit client request as a `.heart-banner` contained panel (bordered, photo on one side, not full-bleed like the mockup) repeating the same 5 core-value icons already detailed on the About page. The `/impeccable critique` pass on 2026-08-13 flagged it directly (Jordan persona note): by the time a first-time parent reaches this section they're looking at a *third* restatement of the same 5 values, with no signal it's intentional reinforcement rather than redundant filler - exactly the section VISUAL_DENSITY 3 argues against keeping. The client reversed the earlier "keep it" call on 2026-08-18 and asked for it cut outright. `.heart-banner*` (blade markup, `method.css` rules) and the now-orphaned `method_heart_banner` image key (`OtherPagesImageGroups`, `MethodController`) are gone, not just hidden.
- **Closing CTA** - `.closing-cta` reused verbatim with page-specific copy, no background photo (no `method_closing_cta_photo` key exists - falls back to flat `--ink`, the component's existing no-photo state, not a gap).

## Page: Hoạt động & Sự kiện (Activity)

Third non-homepage page built out from a stub (previously bare `.section` + `<h1>`, "mục E1–E5 sẽ được xây dựng ở bước tiếp theo"). The user supplied a reference mockup and separate ChatGPT-drafted copy but explicitly granted freedom to depart from both; resolved through a written Interactive Design Pitch + an interactive Claude Artifact preview (clickable filter chips, not just static screenshots) before any project code was touched, following the same approve-before-build discipline as About's timeline redesign.

- **Visual language: re-skinned, not cloned.** The mockup used a purple/navy ground with a different fill color per category icon (orange/blue/green/yellow/purple/red). Rebuilt entirely in the site's existing Ink/Gold/Paper system - one accent hue, categories told apart by icon and label only, never color (Two-Gold Rule, Color Consistency Lock, and the project's own "numbering/icon, never color" Do-rule all apply directly here, not just by analogy). The mockup's other structural idea, a floating filter bar over the banner, was kept in the first build and then removed - see the critique note below.
- **Filter menu - the taxonomy itself was cut to 3, not patched around.** First pass tried to keep all 6 `ActivityCategory` cases in the data model and only promote 3 to filter chips (`workshop`/`summer_camp`/`charity` surfacing under "Tất cả" without their own chip). The client corrected this directly: only 3 categories should exist at all. `ActivityCategory` now has exactly `TOURNAMENT`, `MEETUP`, `FAMILY_DAY` - the other 3 cases are gone from the enum, not just hidden from the UI, so Admin's own "Danh mục" dropdown (`resources/views/admin/activity/_form.blade.php`, which reads `ActivityCategory::cases()`) shrank to the same 3 automatically. The 3 real activities that had been seeded under `workshop`/`summer_camp` were reassigned to the closest of the 3 surviving categories by re-reading each activity's own name and description (not left on a deleted enum value, which would have thrown on the next page load).
- **"Alpha Together" is a label change on a surviving case, not a new category.** `ActivityCategory::FAMILY_DAY->getLabel()` returns "Alpha Together". The enum's underlying value stays `family_day` so existing rows didn't need a data rewrite for this rename specifically, only for the 3 that moved off a deleted case.
- **Pillar Spotlight - new component, state computed from real data, never fabricated.** Three cards for Giải đấu / Giao lưu / Alpha Together. `ActivityController@index` groups `Activity::active()->ordered()->get()` by category and hands each pillar its first matching record (or none). A pillar with a match renders that activity's photo plus a "Xem các kỳ đã qua" link; a pillar with none renders a plain `badge-soon` ("Sắp ra mắt") with no image and no invented copy underneath it - the honest-empty-state rule applied to a whole card, not just a missing photo. Icon roundels reuse `.icon-roundel` (`--accent-soft` fill / `--accent-text` stroke) verbatim from About/Method rather than a new treatment.
- **Archive grid reuses the homepage's activity card, not a new one.** The homepage's featured-activities carousel already had a photo-plus-dark-overlay card (`.activity-card`, `__overlay`, `__tag`, `__media-empty`) living only in `home.css`. Rather than rebuild a near-identical card for this page (the same class-in-the-wrong-file miss that once left the Method page's `.icon-roundel svg` unstyled), moved the whole `.activity-card` family into `base.css` as a shared primitive and gave it a second usage context: `.activity-grid .activity-card` overrides the carousel's fixed `scroll-snap` width into a `flex-wrap` grid item, same visual identity, different container. The homepage carousel itself is untouched.
- **Filter chips are client-side, no new route.** Clicking a chip (`activity.js`, `initActivityFilters()`) shows/hides already-rendered `.activity-card` elements by `data-category` and updates the "Đang lọc: X · N hoạt động" count; a pillar's "Xem các kỳ đã qua" button does the same filter then scrolls to the grid. This directly answers the client's own half-formed idea ("bấm vô có danh sách các lần trước") without a per-activity detail page: each `Activity` already is one edition, and the pillar is the category, so filtering the existing archive by category is the edition list. Filtering to `family_day` today shows the same honest empty state as the pillar card (`.activities__empty.archive-empty`, dashed hairline, no invented content) since no activity carries that category yet - `tournament` and `meetup` both have real activities and filter normally.
- **Post-launch critique (`/impeccable critique`) found three real defects, all fixed in the same pass.** A dual-agent critique run (`.impeccable/critique/2026-08-15T07-03-57Z__resources-views-client-activity-index-blade-php.md`, scored 22/32) caught:
  1. **A confirmed CSS bug**, not a taste call: `.pillar-grid`'s default `align-items: stretch` forces all 3 pillar cards to the height of the tallest (the one with a photo + link), but only that live branch had `.pillar-card__foot{margin-top:auto}` to fill the extra height - the "Sắp ra mắt" branch's `.badge-soon` had no bottom anchor, so it sat under the copy leaving a large dead void, reading as broken rather than intentional. Fixed by giving `.badge-soon` its own `margin-top: auto`, mirroring the live branch's mechanism instead of inventing a new one.
  2. **Three UI surfaces doing one job.** The floating filter bar (top of page), the Pillar Spotlight's "Xem các kỳ đã qua" link, and the archive toolbar's filter state all expressed the same 3-category taxonomy - and the floating bar sat *above* the Pillar Spotlight copy that explains what the categories mean, asking a first-time visitor to filter before they know what they're filtering. Removed the standalone floating bar (`.activity-filter-wrap`/`.activity-filter-bar`, including its hero-overlap `margin-top: -56px`) entirely; the chips now live inside the archive section itself (`.archive-filter-bar`, plain in-flow pills, no shadow/overlap treatment - that visual weight belonged to a real floating element, not a category picker) directly above the grid they control. The Pillar Spotlight keeps its original job: introduce the 3 pillars and link into the (now co-located) filtered view.
  3. **No feedback on chip click, no deep link.** Since the only filter chips left now sit beside the grid they control, a click is self-evidently a fresh section (co-location doubles as feedback, no separate confirmation needed). `activity.js` also now syncs a `?danh-muc=` query param via `history.replaceState` on every filter change and reads it back on load, so a specific category is shareable/bookmarkable without a page reload or a new route.
  - **Gallery ("Khoảnh khắc đáng nhớ") removed outright, not just hidden-when-empty.** It pulled from the same unpaginated `Activity.image` pool the archive grid already renders in full, so it was structurally guaranteed to duplicate content already on screen regardless of how much data Admin ever adds - a "highlights reel" pulling from an unfiltered "everything" list can never be a distinct highlight. Removed the section, the controller's `$galleryPhotos`, and its CSS rather than gate it behind a data threshold that would have delayed the same problem instead of fixing it.
- **Closing CTA** - shared component reused verbatim, gained an optional `activity_closing_cta_photo` key (`OtherPagesImageGroups`) mirroring `about_closing_cta_photo`/`home_closing_cta_photo` exactly; no photo configured falls back to the flat `--ink` field as always.

## Page: Hệ thống cơ sở (Branch)

Fourth non-homepage page built out from a stub. Content came from a reference mockup the user supplied (banner, 4-icon highlight strip floating over the banner, a photo/content/map row per branch, closing CTA), with one explicit change from the mockup (a crossed-out "Đặc điểm" bullet list, matching the earlier decision to drop `features` from Admin) and one new data field the mockup's own text called out.

- **New field: `location` ("Địa điểm cơ sở"), a real column, not a rename.** The user was explicit this sits *beside* `name` in Admin, not replacing it. Card heading became `CƠ SỞ {display order}: {location}` (bold, the number read from iteration order in `Branch::ordered()`, not the row's `id`) with `name` as a smaller secondary line underneath, an intentional 3-line header versus the mockup's 2-line one, since the mockup predates this field. `location` is `nullable` in the migration (the one seeded test branch had none) but `required` in `BranchRequest` going forward; the view falls back to `$branch->name` for the heading and hides the secondary line entirely when `location` is empty, rather than showing a blank line.
- **Floating stat bar - deliberately not built from an existing component, then deliberately shrunk.** The user liked the mockup's floating 4-icon strip over the banner and asked to keep that treatment specifically, overriding an earlier attempt to reuse the `.value-pair` (2-column bordered card, no shadow) pattern instead. Reused `.icon-roundel` for the icons (the one real shared piece), but the floating-card-with-shadow container itself is new to this page - the closest existing precedent, the homepage's Highlight Strip, is a flat `--ink` band with no icons and no shadow, structurally different enough that copying its class would have been the wrong kind of reuse. After the first pass read as too large (32px padding, 52px icons, full <code>--container-max</code> width), shrunk it into a small "connector" card: 18px padding, 36px icons, `max-width: 920px`, positioned with `margin-top: -34px` so it straddles the banner/section seam rather than reading as its own full section. Icon-over-label-over-sub (stacked, centered) instead of icon-beside-text, plus a hairline `border-left` between the four items, both added after explicit "make it clearer" feedback.
- **Lịch học ("Schedule") card grid - grouped by real day, not the mockup's day-range shorthand.** The mockup showed "Thứ 3 – Thứ 5" spanning two days in one box; the actual `schedule` column (built earlier, see Admin's schedule repeater) stores flat `{day, start, end}` rows with no day-range concept, and admins enter one real weekday per row. Inventing a "these two days share the same times, merge them" heuristic would be fragile and diverge from what's actually stored. `Branch::scheduleByDay()` instead groups the flat rows by their real day value and orders the result Thứ 2 → Chủ Nhật (`Branch::DAY_ORDER`) regardless of entry order, so a day with two time slots (e.g. two classes on Thứ 3) renders as one chip with both times stacked, and the chips always read in calendar order even if the admin added Thứ 5 before Thứ 3.
- **"Xem trên bản đồ" - generated from `address`, not a new field.** `Branch::mapsSearchUrl()` builds a `google.com/maps/search` URL from the existing `address` column. The user chose this over adding a separate "Google Maps link" field specifically to avoid growing the Admin form for a value that's already fully derivable. The embedded `<iframe>` (`map_embed_url`, pre-existing) is a separate concern and untouched; a branch can have one, the other, both, or neither, and each degrades independently - no iframe configured shows a dashed-hairline "Chưa cấu hình bản đồ nhúng" box (never a blank gap), no address never renders the maps-search button at all.
- **"Đăng ký học thử" deep-links per branch.** `route('registration.create', ['branch' => $branch->id])` - the registration page is still a stub with no form to read the param yet, so this is a forward-compatible link with no functional effect today, not a half-built feature; wiring the actual pre-selected checkbox is separate work when that page gets built.
- **Reused verbatim:** `.page-hero`/`.page-hero--photo` (banner), `.icon-roundel` (stat bar + nothing else, per above), `.closing-cta` + `.text-accent-free` (closing band, gained the standard optional `branch_closing_cta_photo` key), `.activities__empty` (whole-page empty state when there are zero active branches, the same primitive extracted for the Activity page's archive grid).

## Page: Câu hỏi thường gặp (FAQ)

The most under-built page on the site before this pass: no `page-hero--photo` capability at all (the only page missing its `{page}_banner` key in `OtherPagesImageGroups`, despite `.page-hero`/`.page-hero--photo` already being a shared primitive by the time About shipped), no `.section-head` before its accordion, and its own bespoke closing band (`.faq-body__cta`, a boxed `--ink` card) instead of the shared `.closing-cta` every other inner page had already consolidated onto. A reference mockup (purple/navy, a bespoke icon per question, a "Vẫn còn thắc mắc?" contact sidebar, a "Vì sao chọn Alpha Kids" sidebar) was supplied with explicit freedom to depart from it, resolved via a written Interactive Design Pitch (Claude Artifact, rebuilt from `base.css`'s real measured values, not an approximation) before any project code was touched.

- **Both sidebars cut outright, not carried over trimmed.** The mockup's contact sidebar (hotline, Zalo, email, hours, address) would have restated content the shared `_footer.blade.php` partial already renders on every page, including this one - plus the footer's own `site-footer__quick` lead-capture form sits a few hundred px below it on the same render. The "Vì sao chọn Alpha Kids" sidebar would have restated the homepage's own full Flexible Proof Grid section (`ProofPoint`-backed). Both are the same category of defect already cut twice earlier this pass (Activity's gallery duplicating its own archive grid, Method's heart-banner duplicating About's core values) - content that exists in full elsewhere on the same page or one click away doesn't earn a second, thinner restatement.
- **No per-question icon.** The mockup hand-assigns a distinct icon per question, but `Faq` has no `icon` column and the question count is Admin-managed and unbounded (not the mockup's fixed 10) - a bespoke icon per row doesn't scale to that model without a migration + an Admin icon-picker for a purely decorative field nobody asked for. The accordion's own existing plus/minus glyph (shared primitive, see above) remains the only icon.
- **Filled the gap instead of copying the mockup's banner literally.** Added `faq_banner` and `faq_closing_cta_photo` to `OtherPagesImageGroups` - the same two-key pattern every other inner page already carries, zero new CSS (`page-hero--photo`/`.closing-cta` already handle both states). `Client\FaqController::index()` now fetches an `$images` array exactly like every sibling controller; previously it fetched nothing but `$faqs`.
- **Accordion wrapped in a real `.section`/`.section-head`, not a bare custom div.** `.faq-body` (page-specific wrapper carrying its own `padding-block`) is gone; the accordion now sits inside the shared `.section` + `.section-head--center` pattern every other page uses, with `.faq-body__inner` reduced to just the `max-width: 760px` reading-width constraint the accordion itself needs. `.faq-body__accordion .accordion-item__trigger`'s `17px`/`24px` size bump (pre-existing, page-specific) is kept - it was never part of what needed fixing.
- **Closing CTA swapped for the shared component.** `.faq-body__cta` (boxed `--ink` card, its own copy/markup shape) is gone; replaced by `.closing-cta` verbatim, same as About/Method/Activity/Branch, gaining the standard optional `faq_closing_cta_photo` no-photo-falls-back-to-flat-ink behavior for free.
- **All accordion items closed on load, explicit user override of the shared-primitive default.** See the Accordion primitive entry above - every other surface backed by `initAccordions()` opens its first item by default; this page was told directly not to. Scoped to `faq.blade.php`'s own `aria-expanded` output only, not a mechanism change.
- **`FAQPage` structured data, added same day (`seo-audit` skill finding).** The full-page audit that followed the rebuild caught this page shipping zero JSON-LD despite being the single highest-value candidate for it on the whole site - Google's `FAQPage` rich result can render the questions directly inside the search snippet, and the codebase already had a working precedent to model (`Branch::structuredData()` + `branch/index.blade.php`'s `@graph` script). `Faq::questionData()` returns one `Question`/`acceptedAnswer` node per row (mirrors `Branch::structuredData()`'s per-instance shape); the view wraps all of them in a single `FAQPage`/`mainEntity` object rather than a `@graph` of many top-level nodes, since `FAQPage` schema is inherently one object per page, not one node per item - the one real structural difference from the Branch precedent, not a stylistic one. Skipped entirely (`@if ($faqs->isNotEmpty())`) when there's no content, matching the site's standing rule against emitting anything schema-shaped with nothing real behind it.

## Page: Liên hệ & Đăng ký học thử (Registration)

Sixth non-homepage page built out from a stub (previously bare `.section` + `<h1>`, "mục H1–H5 sẽ được xây dựng ở bước tiếp theo"). Two reference mockups were supplied (a clean version and the same image hand-annotated in red marking what to cut), with explicit design freedom. Resolved by first auditing the *actual* backend (`registrations` table, both `RegistrationRequest` classes, Admin CRUD) before proposing any field, specifically to avoid pitching a form the schema couldn't support - a written Interactive Design Pitch (Claude Artifact) followed, then two explicit yes/no decisions from the client before any migration ran.

- **Every form field checked against the real `registrations` table first, not the mockup.** The mockup's Ngày sinh/Giới tính/Độ tuổi/Chương trình đăng ký/Thời gian quan tâm block looked like five fields; the schema only ever backed `birth_year` (year only, no month/day - matching the client's own handwritten "NĂM" annotation on the mockup) and, after this pass, `gender`. Độ tuổi (a redundant dropdown restating birth year), Thời gian quan tâm, and Chương trình đăng ký had zero backing columns and were cut outright rather than reverse-engineered into new schema nobody asked for - the same annotation had already crossed them out.
- **Two client-facing schema decisions, asked directly instead of guessed.** "Họ và tên phụ huynh" appeared in the mockup unmarked (not crossed), but the table had no column for it either - asked, answer was **cut it, don't add `parent_name`**. Giới tính's crossed/uncrossed state was ambiguous in the annotation - asked, answer was **add it**: new migration `2026_08_18_100000_add_gender_to_registrations_table.php` (`gender` string, nullable, positioned `after('birth_year')`), a new `App\Enums\Gender` (`MALE`/`FEMALE`, `getLabel()` - same string-backed shape as `ActivityCategory`, not the int-backed `RegistrationStatus`, since this is a client-facing value with only two states rather than an admin workflow status), cast on `Registration::$casts`, validated `required` client-side and `nullable` admin-side (mirrors how `birth_year`/`branches` already split required-on-client vs. optional-on-admin), and exposed in the Admin edit form (`admin/registration/_form.blade.php`) so staff can see/correct what a parent submitted.
- **`birth_year` tightened to `required` on the client request only.** The column and the Admin request both stay `nullable` (existing Admin-created rows and any future admin-only flow shouldn't need it) - only `Client\RegistrationRequest` was changed, since this is a "the business wants it collected going forward" decision, not a schema constraint.
- **Stat bar promoted from Branch-only to a shared primitive.** The mockup's 4-icon strip floating over the banner/section seam is not the homepage's plain-text `.highlight-strip` (dark ink band, no icons, a different component) - it's an exact visual match for Branch's own `.stat-bar`/`.stat-item` (white floating card, `icon-roundel` + label + sub, `margin-top: -34px` straddling the seam). Since a second page now needs it, `.stat-wrap`/`.stat-bar`/`.stat-item*` moved from `branch.css` to `base.css` verbatim (same values, zero rewrite) - the same "promote on second use" pattern already applied to `.page-hero`/`.closing-cta`/`.value-pair` earlier in the project's history.
- **No `.closing-cta` band on this page, a deliberate exception.** Every other inner page ends on the shared full-bleed closing CTA; this page's entire reason to exist *is* the registration form, so a second "register now" band below the form's own submit button would ask the same thing twice in one scroll. Neither reference mockup included one either. `OtherPagesImageGroups['registration']` correspondingly keeps only `registration_banner`, no `registration_closing_cta_photo` key.
- **Contact sidebar, kept then reversed same day.** Originally kept (unlike FAQ's identical-looking sidebar, which was cut) on the reasoning that this page's whole job is contact, so a "call instead" affordance is a real alternate path, not restatement. Revisited after the stat-bar removal and found inconsistent with that same call: a full equal-weight card competing with the form for attention *is* the kind of friction-near-the-CTA the stat-bar was cut for, and the underlying footer-duplication argument that sank FAQ's identical sidebar applies here too. Resolved by keeping the one genuinely useful part (the phone number, for the real fraction of parents who'll abandon a form but complete a call) and downgrading its presentation from a full `.contact-card` to `.form-card__nudge` - a single hairline-topped line inside the form card itself ("Muốn nhanh hơn? Gọi hoặc Zalo …"), the same light-touch text-nudge device already established (`.club-intro__branch-link` on About, the "Xem chi tiết tại trang Về CLB" link added to Method) rather than a new component. Address dropped entirely - nothing actionable to do with it on this page (no map, no directions button), and the form's own "Cơ sở đăng ký" checkboxes already surface each branch's location. `.registration-grid` (300px/1fr split) is gone; `.registration-wrap` (`max-width: 720px`, centered) now holds the form alone. `Client\RegistrationController::create()`'s `$contact` array trimmed to just `hotline` - `zalo_contact`/`address` were fetched for a UI that no longer exists.
- **First real multi-field client-side form on the site - new light-ground input language, not reused from the footer.** The footer's `site-footer__quick-form` inputs (`site-footer__quick-form input` in `layout.css`) are white-on-translucent, built for the dark `--ink` footer; this form sits on `--paper`, so `registration.css` defines its own `.field input`/`.field select`/`.field .textarea` (hairline border, `--radius-md`, `--accent-dark` focus ring) rather than forcing the dark-ground pattern onto a light card. Radio (`Giới tính`) and checkbox-as-pill (`Cơ sở đăng ký`) both use `accent-color: var(--accent-text)` for native browser styling rather than custom-drawn inputs - first use of that CSS property on the site, chosen because these are real native form controls (not decorative toggles like the accordion's plus/minus), and native controls should look native.
- **Field names match the footer's quick form exactly.** `child_name`/`phone` are the same `name=` attributes the footer widget has used all along - same field, same name, so a returning visitor filling out the fuller form isn't learning new field semantics.
- **Full-page audit, same day, found 4 real defects and one taste note.** (1) `.stat-wrap` shipped without the `container` class Branch's own copy of this markup always carries - `.stat-wrap` has no padding of its own, so on any viewport under 1200px the stat-bar sat flush against the screen edge. Fixed by adding `container` back. (2) No `<h2>` anywhere on the page - banner `<h1>` jumped straight to the cards' `<h3>`s, the only inner page without a `.section-head` before its main content. Added one ("Sẵn sàng cho buổi học thử đầu tiên?"). (3) `.field-error` was hardcoded `color: #B23` - the first client-facing validation-error color on the site, invented on the spot instead of reusing anything. The Admin panel already had exactly this need solved (`--danger: #C0392B` in `public/css/admin/app.css`); added the same token to the client `:root` in `base.css` and pointed `.field-error` at it, rather than leaving a bare hex outside the token system. (4) The meta description carried an em dash forward from the original stub text - the project's own Don't list bans this; rewritten with a colon.
- **`.page-hero` dropped entirely - the one page on the site without it - after the user asked for an auto-scroll-to-form animation on load.** Talked out of the animation itself first: auto-scrolling on page load with no user action is a real User Control and Freedom violation, a genuine motion-sensitivity risk, and (after everything else already cut from this page) solves a problem that barely exists anymore - the form was already one short scroll from the top. The user's own follow-up question ("what if this page just has no banner?") was the better fix: instead of animating past the hero, remove the reason to scroll past anything. `<h1>` moved into `.form-card__head` (previously an `<h3>`, previously duplicated by the page-hero's own `<h1>` and a redundant `.section-head` `<h2>` saying almost the same thing - both gone now, one heading doing the one job). Trade-off, stated plainly rather than hidden: every other inner page keeps `.page-hero` for a visitor who hasn't decided anything yet; this page's visitor already clicked "Đăng ký học thử" from somewhere, so the context-setting job the hero does elsewhere is already done before they arrive - the same "already decided, minimize friction" reasoning already used twice on this page (stat-bar, contact card). `registration_banner` removed from `OtherPagesImageGroups` (the `'registration'` entry is gone outright - with no closing-CTA photo key either, it had nothing left in it) and from `Client\RegistrationController::create()`, which no longer builds an `$images` array at all.
- **Stat bar removed outright, same day, on reflection.** Kept for one round after the "đơn điệu" fix, then cut per explicit request: Branch's visitor is still evaluating (needs the 4 reassurance USPs before picking a cơ sở), this page's visitor has already decided to register and just needs the fastest path to the form - reassurance chrome right before the form adds friction at exactly the moment it should be minimized, not reduces it. `.stat-wrap`/`.stat-bar`/`.stat-item*` stay in `base.css` (Branch still uses them verbatim); only the markup instance in `registration/create.blade.php` is gone. The new `.section-head`/`<h2>` added earlier now does double duty as the banner→section seam definition the floating stat-bar used to provide.
- **`.section-head` restored per explicit request, `.page-hero` stays removed - resolved as a heading-order fix, not a reversal.** The user pasted back the exact `.section-head`/`<h2>` block that had been cut and asked for it back. Putting it back verbatim would have put an `<h2>` before the page's only `<h1>` (now inside `.form-card__head`) - backwards heading order. Fixed by promoting the section-head's own heading to `<h1>` (it's genuinely the first heading on the page now) and demoting `.form-card__head`'s to `<h2>` - the two no longer collide in meaning either ("Sẵn sàng cho buổi học thử đầu tiên?", a question, vs. "Đăng ký học thử miễn phí," a transactional label), unlike the 3-heading version from earlier the same day where the wording actually did repeat. `.section-head h1` is a page-local override in `registration.css` (the shared `.section-head` in `base.css` only styles `h2`, since every other page uses `.section-head` under a `.page-hero`'s own `h1`).
- **"Trang hơi trống" (client feedback, after `.page-hero` removal) - the top gap and the form itself both widened.** `--section-gap` (120px) was sized to sit *below* a hero; with no hero above it anymore, that padding read as dead space rather than rhythm. Section carries `.section--padding-block-80` now (existing utility, 80px, already used elsewhere for lighter sections - not a new value invented for this page). `.registration-wrap` grew `720px → 880px` and `.form-card` padding `32px/30px → 40px/36px` (mobile keeps the tighter `28px/22px`) - wide enough that the two-up `.form-row` fields (~400px each) read as properly filled inputs rather than the original unbounded-width bug or the over-corrected 720px cap, without returning to the pre-fix sparse-input problem from earlier the same day.
- **"Hơi đơn điệu," round 2, after the contact card and stat-bar were both already cut.** By this point the page was down to a single flat form: 6 fields in one unbroken list, no rhythm. Fixed inside the form itself rather than by adding back a section - reused "Structure is information" (already the reasoning behind the numbered/lettered C.A.R.E cards): the 6 fields split into two real groups (`.form-group`, uppercase `--accent-text` eyebrow label matching `.care-detail-card__subtitle`'s existing typographic role) - "Thông tin của bé" (name, birth year, gender) and "Liên hệ & cơ sở" (phone, branches, note) - separated by a hairline `.form-group--divider`, each staggered in on scroll (`reveal-d1`/`reveal-d2`) instead of the whole card fading in as one flat block. Branch checkboxes gained a small location-pin icon (`--accent-text` stroke, icon only, no color-coding - the numbering/icon-never-color Do's rule applies here as much as anywhere else). Added a one-line reassurance caption under the submit button ("Hoàn toàn miễn phí · Không cần thanh toán trước") - the same gap flagged as a general principle in the Method page's 13/08 critique ("the highest-stakes sentence on the page currently does the least reassurance work"), applied here for the first time at the actual point of highest stakes on the whole site: the literal submit click.
- **"Hơi đơn điệu," round 1 (contact card and stat-bar still present at the time) - two cards of equal visual weight read as flat, not a layout problem.** Fixed by giving the two cards different weight instead of adding new sections (which would have reopened the duplication problem this whole page was built to avoid): `.contact-card` recedes onto `--paper-alt` (secondary, supporting), `.form-card` stays `--white` and gained a `.form-card__head` - an `.icon-roundel` beside the heading, the same icon+heading pairing already used for "Nền tảng phương pháp" on Method and "Tầm nhìn/Sứ mệnh" on About, not a new device. Also capped `.registration-grid` at `max-width: 1040px` (it had no cap at all - on a wide monitor the form column stretched past 1100px, making each half of a two-up `.form-row` roughly 550px for a name/date field, visibly sparse).

## Do's and Don'ts

### Do:
- **Do** check which ground gold sits on before choosing the token - `--accent` for fills/dark-ground text, `--accent-text` for light-ground text (The Two-Gold Rule).
- **Do** use ink text on every gold-filled button and band - never white, never accent-text-on-accent.
- **Do** use numbering/icon, never color, to distinguish items within a "four things" or "three things" grid.
- **Do** keep the hero's scrim strong enough to hold WCAG AA regardless of the uploaded photo's brightness, including at every point of the mesh's drift animation.
- **Do** ship honest empty/no-content states (dashed hairline outline + plain sentence) - never fabricated testimonials, stats, or FAQ content.

### Don't:
- **Don't** introduce a second display/heading font, handwritten or otherwise.
- **Don't** add a second accent hue, even a tint, for a "just this one section" reason.
- **Don't** put white text on a gold fill, or `--accent`-on-paper text - both fail WCAG AA; this has been measured, not eyeballed.
- **Don't** add a colored `border-left`/`border-right` "side-tab" to any card or panel - mark emphasis with a glyph, tint, or weight instead.
- **Don't** add card borders + shadows back onto the flush hairline grids.
- **Don't** repeat a layout family across sections - check the Section-Layout-Repetition list before adding a new "N items in a row" section.
- **Don't** put a feature checklist, trust strip, or pricing teaser inside the hero.
- **Don't** use an em dash (-) anywhere in new copy. Use a comma, period, or colon instead.
